FLASK_APP="dts_app.app:config_app('dev')"

# fichier qui permet de faire le run de l'app c'est le chemin qu'on appelle quand on lance
# FLASK_DEBUG=True flask run
# quand on lance la commande FLASK avec la libraire dotenv permet de faire apparaitre les config qui sont dans
# .env et .flaskenv et ça évite de relancer manuellement à chauqe fois les config quand on lance _app