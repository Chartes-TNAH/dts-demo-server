<?xml version="1.0" encoding="UTF-8" ?>
<TEI>
{% block metadata %}
    <teiHeader>
    <!-- Metadata in teiHeader -->
    <chant_ID>{{ chant_ID }}</chant_ID>
    <Col>{{ Col }}</Col>
    <Collection_parent>{{ Collection_parent }}</Collection_parent>
    <Titre>{{ Titre }}</Titre>
    <Auteur>{{ Auteur }}</Auteur>
    <Interp>{{ Interp }}</Interp>
    <Date_creation>{{ Date_creation }}</Date_creation>
    <Content>{{ Content }}</Content>
    <Link>{{ Link }}</Link>
</teiHeader>
    {% endblock %}
<text>
    {% block corps %}
    <body>
        <!-- englober le texte dans des balises p, insérer une balise l à chaque retour chariot +1 en numérotation -->
        <p>
            <l n="1">
                {{ content.txt }}
            </l>
        </p>
    </body>
    {% endblock %}
</text>
</TEI>